{
  "//": [
    "OPT-IN example — NOT loaded by default. SessionStart (background",
    "marketplace refresh) now ships enabled in the active hooks.json; this",
    "file documents only the remaining opt-in extra: a SessionEnd nudge to run",
    "/learn. Swap the echo for your own reflection script for fully automatic",
    "capture. To enable, copy the hooks block below into hooks.json."
  ],
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'studio: session ended — run /learn to capture any lessons.' >&2"
          }
        ]
      }
    ]
  }
}
